# 用户验收步骤 · 06 出网 LLM 客户端

1. 清空 `env/dev.json` 的 key，构建安装。对小暖发非红旗句「你好」。应看到「未配置模型服务」，不得闪退。
2. 填入无效 key，发「你好」。应在约 15 秒内出现「稍后再试」，界面不得出现 `401` 或英文报错。
3. 填入有效 key（开发机），发「你好」。应出现模型回复（具体内容属 08 人设，本步只验证「不是稍后再试」）。
4. 打开飞行模式发「你好」（非红旗）。应「稍后再试」或等价失败文案（指定为「稍后再试」）。
