# 用户验收步骤 · 04 会话 UI 与消息持久化

1. 「对话」tab 无记录时应看到「还没有对话」。
2. 首页点小暖，输入 `你好` 发送，气泡出现在右侧。
3. 返回再进小暖，`你好` 还在（进程杀掉重开也应在）。
4. 连点发送两次（同一句），库里/界面不得出现两句完全相同的连续用户消息（允许一次发送）。
5. 粘贴超长文字（让开发准备 2001 字），应提示「最多 2000 字」且界面不增加新气泡。
